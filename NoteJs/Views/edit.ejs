<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Note Js</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 text-white">
        <div class="form p-10">
            <% if (note) { %>
                <h1 class="text-center text-2xl font-bold mb-3">Update Note</h1>
                <form action="/edit/<%= note._id %>" method="post">
                    <input value="<%= note.title %>" class="block rounded-md w-full px-5 py-2 outline-none bg-zinc-800" type="text" name="title" id="title" placeholder="Enter Title">
                    <textarea class="block rounded-md w-full resize-none outline-none mt-3 px-5 py-2 bg-zinc-800" type="text" name="data" id="data" placeholder="Enter Data"><%= note.data %></textarea>
                    <button class="mt-3 rounded-md bg-blue-700 px-3 py-2" type="submit">Update</button>
                </form>
            <% }%>
        </div>
    </div>
</body>
</html>